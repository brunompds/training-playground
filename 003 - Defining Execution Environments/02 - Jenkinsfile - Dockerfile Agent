pipeline {
    agent { 
        dockerfile {
            dir 'D:\WORK\TRAINING\jenkins\003 - Defining Execution Environments\02 - Dockerfile - Dockerfile Agent'
            additionalBuildArgs ''
            args ''
        }  
	}
    stages {
        stage('build') {
            steps {
                sh 'if [ "$ENV_VARIABLE" != "Value for execution" ]; then exit 1; fi'
            }
        }
    }
}