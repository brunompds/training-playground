pipeline {
    agent { 
        dockerfile {
            dir '003 - Defining Execution Environments'
            filename '02 - Dockerfile - Dockerfile Agent'
            additionalBuildArgs ''
            args ''
        }  
	}
    stages {
        stage('build') {
            steps {
                sh 'if [ "$ENV_VARIABLE" != "Value for execution" ]; then exit 1; fi'
            }
        }
    }
}

/var/jenkins_home/workspace/usr1_workspace/00302/003 - Defining Execution Environments/02 - Dockerfile - Dockerfile Agent/Dockerfile: Not a directory