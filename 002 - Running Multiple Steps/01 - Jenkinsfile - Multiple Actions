pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
		stage('data'){
			steps{
				sh 'date > ficheiro.txt'
				sh 'cat ficheiro.txt'
				sh 'sleep 10'
				sh 'cat ficheiro.txt'
			}
		}
    }
}