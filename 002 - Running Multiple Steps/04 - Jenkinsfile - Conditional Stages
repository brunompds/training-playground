pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
		stage('build'){
			when{
				expression { 
					return params.ENVIRONMENT == 'build'
				}
			}
			steps{
				echo 'build'
			}
		}
		stage('test'){
			when{
				expression { 
					return params.ENVIRONMENT == 'test'
				}
			}
			steps{
				echo 'test'
			}
		}
    }
}